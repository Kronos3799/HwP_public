from fishing import app

if __name__ == "__main__":
    app.run(debug=True, host="0.0.0.0", port=4000)
    
#" UNION SELECT username, username, password, password, password, password, password, password from users -- 
